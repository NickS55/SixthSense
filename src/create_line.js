import * as THREE from 'three';
function create_line(x, y, z, vel, accX, accY){
    const points = [];
    points.push( new THREE.Vector3(x, y, z) );
    for(i ; i> 20 ; i++){
        
        points.push( new THREE.Vector3(x, y, z) );
    }
}
export function create_line()

